//- Merge Style Objects
-var mergeStyles = function(obj1, obj2) {
-	var obj = {};
-	for(var attr in obj1) {
-		obj[attr] = obj1[attr];
-	}
-	for(var attr in obj2) {
-		obj[attr] = obj2[attr];
-	}
-	return obj;
-};

//- Translate a key
- function translate(key){
-
- 	// If language is not defined
- 	if (!lang) return 'Language not defined'
-
- 	
- 	var items = dictionary.item;
- 	for(var i = 0; i < items.length; i++){
- 		var item = items[i]
- 		if (item.$.key === lang + '-' + key) return item._
- 	}
-
- 	// No key found, return composed key
- 	return lang + '-' + key
- }